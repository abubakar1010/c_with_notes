#include <stdio.h>

int main()

{
    // int score;
    // float ant;
    // char a;
    // scanf("%d %f %c",&score, &ant, &a);

    // printf("%d %0.4f %c", score, ant, a);

    int a,b;
    // char p;

    // scanf("%d%c %d%c", &a,&p,&b,&p);

    // printf("%d%% %d%%",a,b);
    scanf("%d%% %d%%", &a, &b);

    printf("%d%% %d%%", a, b);
}